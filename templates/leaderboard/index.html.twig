{% extends 'base.html.twig' %}

{% block title %}Tabla de posiciones{% endblock %}

{% block body %}
<div class="d-flex flex-wrap justify-content-between align-items-center mb-3 gap-2">
    <h1 class="h3 mb-0">Tabla de posiciones</h1>
</div>

{% if nextFixture %}
    <div class="card shadow-sm mb-3">
        <div class="card-body d-flex flex-wrap justify-content-between align-items-center gap-2">
            <div>
                <div class="fw-semibold">Próximo partido a cerrar</div>
                <div>
                    {{ nextFixture.homeTeam.code|flag_icon }} {{ nextFixture.homeTeam.code|country_name_es(nextFixture.homeTeam.name) }}
                    vs
                    {{ nextFixture.awayTeam.code|flag_icon }} {{ nextFixture.awayTeam.code|country_name_es(nextFixture.awayTeam.name) }}
                </div>
                <small class="text-body-secondary d-block mt-1">Ese es el partido que más pronto cierra para cargar o editar tu pronóstico.</small>
            </div>
            <small class="text-body-secondary">
                Cierre: <span class="js-local-datetime" data-utc="{{ nextFixture.kickoffAt|date_modify('-5 minutes')|date('c') }}">{{ nextFixture.kickoffAt|date_modify('-5 minutes')|date('Y-m-d H:i') }} UTC</span>
            </small>
        </div>
    </div>
{% endif %}

{{ include('leaderboard/_locked_winners.html.twig', {lockedWinners: lockedWinners}) }}

<div class="row g-4 align-items-start">
    <div class="col-lg-4">
        <div class="mb-2 d-flex justify-content-between align-items-center gap-2 flex-wrap">
            <h2 class="h5 mb-0">Usuarios</h2>
            <span class="badge bg-primary">Faltan {{ remainingCount }} de {{ totalMatchCount }} partido{{ totalMatchCount == 1 ? '' : 's' }}</span>
        </div>
        <p class="screen-note mb-2">Los <strong>+X</strong> son puntos en vivo antes del cierre. El ranking oficial se fija cuando el partido finaliza.</p>
        <svg style="display: none;">
            <defs>
                <linearGradient id="goldMedalGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" stop-color="#fff2a3" />
                    <stop offset="50%" stop-color="#f5b700" />
                    <stop offset="100%" stop-color="#b8860b" />
                </linearGradient>
                <linearGradient id="silverMedalGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" stop-color="#ffffff" />
                    <stop offset="50%" stop-color="#cccccc" />
                    <stop offset="100%" stop-color="#8e8e8e" />
                </linearGradient>
                <linearGradient id="bronzeMedalGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" stop-color="#ffd5bf" />
                    <stop offset="50%" stop-color="#cd7f32" />
                    <stop offset="100%" stop-color="#8c4f1a" />
                </linearGradient>
            </defs>
        </svg>
        <div class="table-responsive bg-white border rounded-3 shadow-sm">
            <table class="table mb-0">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Participante</th>
                        <th class="text-center">Puntos</th>
                        <th class="text-center">Aciertos exactos</th>
                    </tr>
                </thead>
                <tbody>
                    {% for row in rows %}
                        {% set livePoints = livePointsByUser[row.userId]|default(0) %}
                        {% set streak = streakByUser[row.userId]|default(0) %}
                        {% set rowClasses = [] %}
                        {% if app.user and row.userId == app.user.id %}
                            {% set rowClasses = rowClasses|merge(['table-row-current-user']) %}
                        {% endif %}
                        {% if row.lockedPlace is defined and row.lockedPlace %}
                            {% set rowClasses = rowClasses|merge(['table-row-locked-place-' ~ row.lockedPlace]) %}
                        {% endif %}
                        <tr class="{{ rowClasses|join(' ') }}">
                            <td>
                                {% if row.rank == 1 %}
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="22" height="22" class="flex-shrink-0" style="vertical-align: -0.25em;">
                                        <path d="M8 11L5 2h5l2 5 2-5h5l-3 9z" fill="#d32f2f" />
                                        <path d="M10 2h4v8h-4z" fill="#ffffff" />
                                        <circle cx="12" cy="15" r="6" fill="url(#goldMedalGrad)" stroke="#ffd700" stroke-width="0.8" />
                                        <circle cx="12" cy="15" r="4.5" fill="none" stroke="#ffffff" stroke-width="0.6" opacity="0.4" />
                                        <text x="12" y="15" font-family="'Manrope', 'Segoe UI', sans-serif" font-size="9" font-weight="900" fill="#ffffff" stroke="#5c4300" stroke-width="0.6" paint-order="stroke fill" text-anchor="middle" dominant-baseline="central">1</text>
                                    </svg>
                                {% elseif row.rank == 2 %}
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="22" height="22" class="flex-shrink-0" style="vertical-align: -0.25em;">
                                        <path d="M8 11L5 2h5l2 5 2-5h5l-3 9z" fill="#1976d2" />
                                        <path d="M10 2h4v8h-4z" fill="#ffffff" />
                                        <circle cx="12" cy="15" r="6" fill="url(#silverMedalGrad)" stroke="#e0e0e0" stroke-width="0.8" />
                                        <circle cx="12" cy="15" r="4.5" fill="none" stroke="#ffffff" stroke-width="0.6" opacity="0.4" />
                                        <text x="12" y="15" font-family="'Manrope', 'Segoe UI', sans-serif" font-size="9" font-weight="900" fill="#ffffff" stroke="#333333" stroke-width="0.6" paint-order="stroke fill" text-anchor="middle" dominant-baseline="central">2</text>
                                    </svg>
                                {% elseif row.rank == 3 %}
                                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="22" height="22" class="flex-shrink-0" style="vertical-align: -0.25em;">
                                        <path d="M8 11L5 2h5l2 5 2-5h5l-3 9z" fill="#388e3c" />
                                        <path d="M10 2h4v8h-4z" fill="#ffffff" />
                                        <circle cx="12" cy="15" r="6" fill="url(#bronzeMedalGrad)" stroke="#c5835c" stroke-width="0.8" />
                                        <circle cx="12" cy="15" r="4.5" fill="none" stroke="#ffffff" stroke-width="0.6" opacity="0.4" />
                                        <text x="12" y="15" font-family="'Manrope', 'Segoe UI', sans-serif" font-size="9" font-weight="900" fill="#ffffff" stroke="#3e1f00" stroke-width="0.6" paint-order="stroke fill" text-anchor="middle" dominant-baseline="central">3</text>
                                    </svg>
                                {% else %}
                                    {{ row.rank }}
                                {% endif %}
                            </td>
                            <td>
                                {% if row.rank == 1 %}
                                    <strong style="color: #D4AF37;"><a href="#" class="js-open-user-audit text-decoration-none" data-user-id="{{ row.userId }}" data-user-name="{{ row.name }}" style="color: inherit; cursor: pointer;">{{ row.name }}</a></strong>
                                {% elseif row.rank == 2 %}
                                    <strong style="color: #C0C0C0;"><a href="#" class="js-open-user-audit text-decoration-none" data-user-id="{{ row.userId }}" data-user-name="{{ row.name }}" style="color: inherit; cursor: pointer;">{{ row.name }}</a></strong>
                                {% elseif row.rank == 3 %}
                                    <strong style="color: #CD7F32;"><a href="#" class="js-open-user-audit text-decoration-none" data-user-id="{{ row.userId }}" data-user-name="{{ row.name }}" style="color: inherit; cursor: pointer;">{{ row.name }}</a></strong>
                                {% else %}
                                    <a href="#" class="js-open-user-audit text-decoration-none" data-user-id="{{ row.userId }}" data-user-name="{{ row.name }}" style="color: inherit; cursor: pointer;">{{ row.name }}</a>
                                {% endif %}
                                {% if row.lockedPlace is defined and row.lockedPlace %}
                                    <span class="badge locked-place-badge locked-place-badge-{{ row.lockedPlace }} ms-1">Definido</span>
                                {% endif %}
                            </td>
                            <td class="text-center">
                                <strong>{{ row.points }}</strong>
                                {% if livePoints > 0 %}
                                    <span class="text-info fw-semibold">+{{ livePoints }}</span>
                                {% endif %}
                            </td>
                            <td class="text-center">{{ row.exactHits }}</td>
                        </tr>
                    {% else %}
                        <tr><td colspan="4" class="text-center py-4">Sin datos todavía.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    <div class="col-lg-8">
        <div class="row g-3 mb-4">
            <div class="col-12 col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-header fw-semibold d-flex justify-content-between align-items-center gap-2">
                        <span>Próximos partidos</span>
                        <button
                            type="button"
                            class="btn btn-outline-dark btn-sm"
                            data-open-predictions="next-scheduled-predictions"
                        >Ver predicciones</button>
                    </div>
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-sm align-middle mb-0 group-fixtures-table">
                                <tbody>
                                    {% for fixture in nextScheduled %}
                                        {% set kickoffTs = fixture.kickoffAt|date('U') %}
                                        {% set nowTs = 'now'|date('U') %}
                                        {% set hasLoadedScore = fixture.homeScore is not null and fixture.awayScore is not null %}
                                        {% set isInProgress = fixture.status == 'scheduled' and (kickoffTs <= nowTs or hasLoadedScore) %}
                                        {% set isFinished = fixture.status == 'finished' %}
                                        <tr>
                                            <td class="fw-semibold team-cell team-cell-home">
                                                {{ fixture.homeTeam.code|flag_icon }} {{ fixture.homeTeam.code|country_name_es(fixture.homeTeam.name) }}
                                            </td>
                                            <td class="text-center fw-bold score-cell">
                                                {% if isFinished %}
                                                    <small class="d-block mb-1"><span class="badge text-bg-secondary">Finalizado</span></small>
                                                {% elseif isInProgress %}
                                                    <small class="d-block mb-1"><span class="badge text-bg-info">En curso</span></small>
                                                {% endif %}
                                                {% if hasLoadedScore %}
                                                    {{ fixture.homeScore }} - {{ fixture.awayScore }}
                                                    {% if fixture.wentToPenalties %}
                                                        <small class="d-block text-body-secondary fw-normal">({{ fixture.penaltyHomeScore }} - {{ fixture.penaltyAwayScore }} pen.)</small>
                                                    {% endif %}
                                                {% else %}
                                                    &nbsp;
                                                {% endif %}
                                                <small class="d-block text-body-secondary fw-normal mt-1 js-local-datetime kickoff-time" data-utc="{{ fixture.kickoffAt|date('c') }}">{{ fixture.kickoffAt|date('Y-m-d H:i') }} UTC</small>
                                            </td>
                                            <td class="fw-semibold text-end team-cell team-cell-away">
                                                {{ fixture.awayTeam.code|country_name_es(fixture.awayTeam.name) }} {{ fixture.awayTeam.code|flag_icon }}
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="3" class="text-center py-3 text-body-secondary small">No hay más partidos programados.</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-header fw-semibold d-flex justify-content-between align-items-center gap-2">
                        <span>Últimos partidos finalizados</span>
                        <button
                            type="button"
                            class="btn btn-outline-dark btn-sm"
                            data-open-predictions="latest-finished-predictions"
                        >Ver predicciones</button>
                    </div>
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-sm align-middle mb-0 group-fixtures-table">
                                <tbody>
                                    {% for fixture in latestFinished %}
                                        {% set kickoffTs = fixture.kickoffAt|date('U') %}
                                        {% set nowTs = 'now'|date('U') %}
                                        {% set hasLoadedScore = fixture.homeScore is not null and fixture.awayScore is not null %}
                                        {% set isInProgress = fixture.status == 'scheduled' and (kickoffTs <= nowTs or hasLoadedScore) %}
                                        {% set isFinished = fixture.status == 'finished' %}
                                        <tr>
                                            <td class="fw-semibold team-cell team-cell-home">
                                                {{ fixture.homeTeam.code|flag_icon }} {{ fixture.homeTeam.code|country_name_es(fixture.homeTeam.name) }}
                                            </td>
                                            <td class="text-center fw-bold score-cell">
                                                {% if isFinished %}
                                                    <small class="d-block mb-1"><span class="badge text-bg-secondary">Finalizado</span></small>
                                                {% elseif isInProgress %}
                                                    <small class="d-block mb-1"><span class="badge text-bg-info">En curso</span></small>
                                                {% endif %}
                                                {% if hasLoadedScore %}
                                                    {{ fixture.homeScore }} - {{ fixture.awayScore }}
                                                    {% if fixture.wentToPenalties %}
                                                        <small class="d-block text-body-secondary fw-normal">({{ fixture.penaltyHomeScore }} - {{ fixture.penaltyAwayScore }} pen.)</small>
                                                    {% endif %}
                                                {% else %}
                                                    &nbsp;
                                                {% endif %}
                                                <small class="d-block text-body-secondary fw-normal mt-1 js-local-datetime kickoff-time" data-utc="{{ fixture.kickoffAt|date('c') }}">{{ fixture.kickoffAt|date('Y-m-d H:i') }} UTC</small>
                                            </td>
                                            <td class="fw-semibold text-end team-cell team-cell-away">
                                                {{ fixture.awayTeam.code|country_name_es(fixture.awayTeam.name) }} {{ fixture.awayTeam.code|flag_icon }}
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="3" class="text-center py-3 text-body-secondary small">Ningún partido finalizado todavía.</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% include 'leaderboard/_fixture_predictions_modal.html.twig' with {
            modalId: 'next-scheduled-predictions',
            modalTitle: 'Predicciones — Próximos partidos',
            predictionsByFixture: predictionsByNextScheduled,
            emptyMessage: 'No hay predicciones de partidos cerrados entre los próximos partidos.',
        } %}
        {% include 'leaderboard/_fixture_predictions_modal.html.twig' with {
            modalId: 'latest-finished-predictions',
            modalTitle: 'Predicciones — Últimos partidos finalizados',
            predictionsByFixture: predictionsByLatestFinished,
            emptyMessage: 'No hay predicciones de partidos cerrados entre los últimos partidos finalizados.',
        } %}

        <h2 class="h5 mb-2">Partidos por grupo</h2>
        <div class="row g-3">
            {% for groupCode, fixtures in fixturesByGroup %}
                {% set modalId = 'group-predictions-' ~ loop.index %}
                <div class="col-12 col-md-6">
                    <div class="card shadow-sm h-100">
                        <div class="card-header fw-semibold d-flex justify-content-between align-items-center gap-2">
                            <span>{{ fixtures|first.group ? fixtures|first.group.name : ('Grupo ' ~ groupCode) }}</span>
                            <button
                                type="button"
                                class="btn btn-outline-dark btn-sm"
                                data-open-predictions="{{ modalId }}"
                            >Ver predicciones</button>
                        </div>
                        <div class="card-body p-0">
                            <div class="table-responsive">
                                <table class="table table-sm align-middle mb-0 group-fixtures-table">
                                    <tbody>
                                        {% for fixture in fixtures %}
                                            {% set kickoffTs = fixture.kickoffAt|date('U') %}
                                            {% set nowTs = 'now'|date('U') %}
                                            {% set hasLoadedScore = fixture.homeScore is not null and fixture.awayScore is not null %}
                                            {% set isInProgress = fixture.status == 'scheduled' and (kickoffTs <= nowTs or hasLoadedScore) %}
                                            {% set isFinished = fixture.status == 'finished' %}
                                            <tr>
                                                <td class="fw-semibold team-cell team-cell-home">
                                                    {{ fixture.homeTeam.code|flag_icon }} {{ fixture.homeTeam.code|country_name_es(fixture.homeTeam.name) }}
                                                </td>
                                                <td class="text-center fw-bold score-cell">
                                                    {% if isFinished %}
                                                        <small class="d-block mb-1"><span class="badge text-bg-secondary">Finalizado</span></small>
                                                    {% elseif isInProgress %}
                                                        <small class="d-block mb-1"><span class="badge text-bg-info">En curso</span></small>
                                                    {% endif %}
                                                    {% if hasLoadedScore %}
                                                        {{ fixture.homeScore }} - {{ fixture.awayScore }}
                                                        {% if fixture.wentToPenalties %}
                                                            <small class="d-block text-body-secondary fw-normal">({{ fixture.penaltyHomeScore }} - {{ fixture.penaltyAwayScore }} pen.)</small>
                                                        {% endif %}
                                                    {% else %}
                                                        &nbsp;
                                                    {% endif %}
                                                    <small class="d-block text-body-secondary fw-normal mt-1 js-local-datetime kickoff-time" data-utc="{{ fixture.kickoffAt|date('c') }}">{{ fixture.kickoffAt|date('Y-m-d H:i') }} UTC</small>
                                                </td>
                                                <td class="fw-semibold text-end team-cell team-cell-away">
                                                    {{ fixture.awayTeam.code|country_name_es(fixture.awayTeam.name) }} {{ fixture.awayTeam.code|flag_icon }}
                                                </td>
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="prediction-modal" id="{{ modalId }}" aria-hidden="true" role="dialog" aria-modal="true" aria-labelledby="{{ modalId }}-title">
                    <div class="prediction-modal__backdrop" data-close-predictions="{{ modalId }}"></div>
                    <div class="prediction-modal__panel" role="document">
                        <div class="prediction-modal__header d-flex justify-content-between align-items-center gap-2">
                            <h3 class="h5 mb-0" id="{{ modalId }}-title">Predicciones Grupo {{ groupCode }}</h3>
                            <button type="button" class="btn btn-outline-dark btn-sm" data-close-predictions="{{ modalId }}">Cerrar</button>
                        </div>
                        <div class="prediction-modal__body">
                            <p class="text-body-secondary small mb-3">Aquí solo se muestran predicciones de partidos ya cerrados a edición.</p>
                            {% include 'leaderboard/_fixture_predictions_table.html.twig' with {
                                predictionsByFixture: predictionsByGroup[groupCode]|default([]),
                                emptyMessage: 'No hay predicciones de partidos cerrados en este grupo.',
                            } %}
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12">
                    <div class="alert alert-info mb-0">No hay partidos cargados todavía.</div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>

<div class="prediction-modal" id="user-audit-modal" aria-hidden="true" role="dialog" aria-modal="true" aria-labelledby="user-audit-modal-title">
    <div class="prediction-modal__backdrop" data-close-predictions="user-audit-modal"></div>
    <div class="prediction-modal__panel" role="document">
        <div class="prediction-modal__header d-flex justify-content-between align-items-center gap-2">
            <h3 class="h5 mb-0" id="user-audit-modal-title" style="color: var(--wc-accent);">Auditoría</h3>
            <button type="button" class="btn btn-outline-light btn-sm" data-close-predictions="user-audit-modal">Cerrar</button>
        </div>
        <div class="prediction-modal__body" id="user-audit-modal-body">
            <!-- Cargado dinámicamente -->
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        (() => {
            if (window.__groupPredictionsModalBound) {
                return;
            }
            window.__groupPredictionsModalBound = true;

            const openModal = (id) => {
                const modal = document.getElementById(id);
                if (!modal) {
                    return;
                }

                modal.classList.add('is-open');
                modal.setAttribute('aria-hidden', 'false');
                document.body.classList.add('has-open-modal');
            };

            const closeModal = (id) => {
                const modal = document.getElementById(id);
                if (!modal) {
                    return;
                }

                modal.classList.remove('is-open');
                modal.setAttribute('aria-hidden', 'true');

                if (!document.querySelector('.prediction-modal.is-open')) {
                    document.body.classList.remove('has-open-modal');
                }
            };

            const openUserAudit = (userId, userName) => {
                const titleEl = document.getElementById('user-audit-modal-title');
                const bodyEl = document.getElementById('user-audit-modal-body');
                if (titleEl) {
                    titleEl.textContent = `Auditoría: ${userName}`;
                }
                if (bodyEl) {
                    bodyEl.innerHTML = `
                        <div class="text-center py-5" style="color: var(--wc-muted);">
                            <div class="spinner-border spinner-border-sm me-2 text-info" role="status"></div>
                            <span>Cargando auditoría...</span>
                        </div>
                    `;
                }
                openModal('user-audit-modal');

                fetch(`/leaderboard/user/${userId}/audit`)
                    .then(response => {
                        if (!response.ok) {
                            throw new Error('Error al cargar la auditoría');
                        }
                        return response.text();
                    })
                    .then(html => {
                        if (bodyEl) {
                            bodyEl.innerHTML = html;
                        }
                    })
                    .catch(err => {
                        if (bodyEl) {
                            bodyEl.innerHTML = `<div class="alert alert-danger m-3">${err.message}</div>`;
                        }
                    });
            };

            document.addEventListener('click', (event) => {
                const auditTrigger = event.target.closest('.js-open-user-audit');
                if (auditTrigger) {
                    event.preventDefault();
                    const userId = auditTrigger.dataset.userId;
                    const userName = auditTrigger.dataset.userName;
                    openUserAudit(userId, userName);
                    return;
                }

                const openTrigger = event.target.closest('[data-open-predictions]');
                if (openTrigger) {
                    openModal(openTrigger.dataset.openPredictions);
                    return;
                }

                const closeTrigger = event.target.closest('[data-close-predictions]');
                if (closeTrigger) {
                    closeModal(closeTrigger.dataset.closePredictions);
                }
            });

            document.addEventListener('keydown', (event) => {
                if (event.key !== 'Escape') {
                    return;
                }

                document.querySelectorAll('.prediction-modal.is-open').forEach((modal) => {
                    closeModal(modal.id);
                });
            });
        })();
    </script>
{% endblock %}
