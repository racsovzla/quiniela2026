{% extends 'base.html.twig' %}

{% block title %}Equipo{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h1 class="h4 mb-3">Equipo</h1>
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-primary mt-3">Guardar</button>
                    <a class="btn btn-link" href="{{ path('admin_dashboard') }}">Volver</a>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
